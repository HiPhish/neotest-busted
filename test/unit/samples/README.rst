.. default-role:: code

This directory contains samples taken from various test runs.  The file names
don't end in `_spec.lua`, so they won't be run as tests.  All data is taken
from real test runs to ensure that the data is as accurate as possible.
